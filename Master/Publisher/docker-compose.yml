version: '3'
services:
  rabbitmqsubscriber:
    build:
      context: .
      dockerfile: RabbitmqSubscriber/Dockerfile
    ports:
      - 8081:80
    depends_on:
      - rabbitmq
    networks:
      - mynetwork
  publisher:
    build:
      context: .
      dockerfile: Publisher/Dockerfile
    ports:
      - 8080:80
    depends_on:
      - rabbitmq
    networks:
      - mynetwork
  restclient:
    build:
      context: .
      dockerfile: RestClient/Dockerfile
    ports:
      - 8082:80
    networks:
      - mynetwork
  azureservicebussubscriber:
    build:
      context: .
      dockerfile: AzureServiceBusSubscriber/Dockerfile
    ports:
      - 8083:80
    networks:
      - mynetwork

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
